"use client";

import React from "react";
import { appRoutes, AppRoute } from "@/constants/routes"; // Import from new constants file

// The AppRoute interface is now imported from src/constants/routes.ts
// No need to redefine it here.

// The appRoutes array is now imported from src/constants/routes.ts
// No need to define it here.

export { appRoutes, AppRoute };